services:
  - type: web
    name: safety-server
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y python3 python3-pip
      pip3 install opencv-python
      npm install
    startCommand: node server.js
